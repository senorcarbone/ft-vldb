%!TEX root = main.tex


\section{Integration}

\subsection{Durable Backends}
 
\paris{Several new features are built on rocksdb (snapshots, compaction, incremental snapshots etc). We do not need to say many details, other than how everything integrates together I think. This is really up to discussion}
